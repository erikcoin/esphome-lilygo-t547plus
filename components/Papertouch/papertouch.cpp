#include "touch_component.h"

namespace esphome {
namespace papertouch {

// Your `setup()` and `update()` methods are implemented in the header file
// No additional code is needed here for this example

}  // namespace papertouch
}  // namespace esphome
